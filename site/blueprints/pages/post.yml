title: Post
num: '{{ page.date.toDate("Ymd") }}'

columns:
  main:
    width: 2/3
    sections:
      fields:
        type: fields
        fields:
          metadesc:
            type: textarea
            size: small
          text:
            type: textarea
            size: huge

  sidebar:
    width: 1/3
    sections:
      metafields:
        type: fields
        fields:
          date:
            type: date

      files:
        type: files
